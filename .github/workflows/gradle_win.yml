# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Java Gradle Windows

on:
  push:
    branches: [ "wip-3.0.2" ]
    paths:
      - libs/src/java/**
      - libs/test/*java*/**
      - libs/projects/salmon-libs-gradle/**
  pull_request:
    branches: [ "wip-3.0.2" ]

jobs:
  build:

    runs-on: windows-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: libs/projects/salmon-libs-gradle

    # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
    # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
    steps:
    - name: Checkout sources
      uses: actions/checkout@v4
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: 11
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v5

    # build
    - name: Build Salmon native with Gradle Wrapper
      run: .\gradlew.bat build -x test -i  

    # tests
    # core
    - name: Salmon.Core Default Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonCoreTests" -DAES_PROVIDER_TYPE=Default -DENC_THREADS=1 -i
    - name: Salmon.Core Default Multithreaded Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonCoreTests" -DAES_PROVIDER_TYPE=Default -DENC_THREADS=2 -i
      
    - name: Salmon.Core Aes Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonCoreTests" -DAES_PROVIDER_TYPE=Aes -DENC_THREADS=1 -i
    - name: Salmon.Core Aes Multithreaded Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonCoreTests" -DAES_PROVIDER_TYPE=Aes -DENC_THREADS=2 -i

    - name: Salmon.Core AesIntrinsics Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonCoreTests" -DAES_PROVIDER_TYPE=Aes -DENC_THREADS=1 -i
    - name: Salmon.Core AesIntrinsics Multithreaded Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonCoreTests" -DAES_PROVIDER_TYPE=AesIntrinsics -DENC_THREADS=2 -i


    # fs     
    - name: Salmon.FS Test
      run: .\gradlew.bat :salmon-fs:test --tests "com.mku.salmon.test.SalmonFSTests" -DENC_THREADS=1 -i -DtestDir="C:/tmp/salmon/test"
    - name: Salmon.FS Multithreaded Test
      run: .\gradlew.bat :salmon-fs:test --tests "com.mku.salmon.test.SalmonFSTests" -DENC_THREADS=2 -i -DtestDir="C:/tmp/salmon/test"
      
    # native
    - name: Salmon.Native Aes Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonNativeTests" -DAES_PROVIDER_TYPE=Aes -DENC_THREADS=1 -i
    - name: Salmon.Native Aes Multithreaded Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonNativeTests" -DAES_PROVIDER_TYPE=Aes -DENC_THREADS=2 -i
      
    - name: Salmon.Native AesIntrinsics Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonNativeTests" -DAES_PROVIDER_TYPE=AesIntrinsics -DENC_THREADS=1 -i
    - name: Salmon.Native AesIntrinsics Multithreaded Test
      run: .\gradlew.bat :salmon-core:test --tests "com.mku.salmon.test.SalmonNativeTests" -DAES_PROVIDER_TYPE=AesIntrinsics -DENC_THREADS=2 -i
  
